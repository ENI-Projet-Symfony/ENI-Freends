{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Mon profil  {% endblock %}

{% block body %}
    {% if app.user %}
    <h1 class="text-center">{{ user.pseudo }}</h1>
    <div class="row">
        <div class="col-4">
            MA PHOTO
        </div>
        <div class="col-8">
            <div class="row">
                <div class="col-6 text-center">Prenom : </div>
                <div class="col-6 text-center">{{ user.prenom }}</div>
                <div class="col-6 text-center">Nom : </div>
                <div class="col-6 text-center">{{ user.nom }}</div>
                {% if user.telephone %}
                    <div class="col-6 text-center">Téléphone : </div>
                    <div class="col-6 text-center">{{ user.telephone }}</div>
                {% endif %}
                <div class="col-6 text-center">Email : </div>
                <div class="col-6 text-center">{{ user.mail }}</div>
                <div class="col-6 text-center">Campus : </div>
                <div class="col-6 text-center">{{ user.campus }}</div>
            </div>
        </div>
    </div>
    {% endif %}
    <div class="row">
        <div class="col-12 text-center">
            <a href="{{ path('participant_modification_profil') }}">
                <button type="button" class="mt-4 btn btn-outline-dark"> Modifier mon profil</button>
            </a>
        </div>
    </div>
{% endblock %}



